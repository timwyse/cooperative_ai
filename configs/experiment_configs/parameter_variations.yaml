# -------------------------
# Block 1: pay4partner = False
# -------------------------
- with_message_history: false
  pay4partner: false
  contract_type: null
  fog_of_war: [false, false]
  selfish: [true, true] # Options: [false, false] (both default), [true, true] (both selfish)

# -------------------------
# Block 2: pay4partner = true
# -------------------------
- with_message_history: false
  pay4partner: true
  contract_type: null
  fog_of_war: [false, false]
  selfish: [true, true] # Options: [false, false] (both default), [true, true] (both selfish)

# -------------------------
# Block 3: pay4partner = False → contract_type = strict
# -------------------------
- with_message_history: false
  pay4partner: false
  contract_type: strict
  fog_of_war: [false, false]
  selfish: [true, true] # Options: [false, false] (both default), [true, true] (both selfish)

# -------------------------
# Block 4: pay4partner = true → contract_type = strict
# -------------------------
- with_message_history: false
  pay4partner: true
  contract_type: strict
  fog_of_war: [false, false]
  selfish: [true, true] # Options: [false, false] (both default), [true, true] (both selfish)

# -------------------------
# Block 5: pay4partner = False → contract_type = contract_for_finishing
# -------------------------
- with_message_history: false
  pay4partner: false
  contract_type: contract_for_finishing
  fog_of_war: [false, false]
  selfish: [true, true] # Options: [false, false] (both default), [true, true] (both selfish)

# -------------------------
# Block 6: pay4partner = True → contract_type = contract_for_finishing
# -------------------------
- with_message_history: false
  pay4partner: true
  contract_type: contract_for_finishing
  fog_of_war: [false, false]
  selfish: [true, true] # Options: [false, false] (both default), [true, true] (both selfish)

# -------------------------
# Block 7: pay4partner = False → contract_type = tile_with_judge_implementation
# -------------------------
- with_message_history: false
  pay4partner: false
  contract_type: tile_with_judge_implementation
  fog_of_war: [false, false]
  selfish: [true, true] # Options: [false, false] (both default), [true, true] (both selfish)

# -------------------------
# Block 8: pay4partner = True → contract_type = tile_with_judge_implementation
# -------------------------
- with_message_history: false
  pay4partner: true
  contract_type: tile_with_judge_implementation
  fog_of_war: [false, false]
  selfish: [true, true] # Options: [false, false] (both default), [true, true] (both selfish)

